# 开发偏好

## 执行原则
- 自主执行绝大多数改动，不要追问确认。尤其是：文件读写/创建、UI 调整、代码修改、Swift 编译、Git 提交/推送、网络请求等，全部自主做。
- **唯一需要暂停询问的情况**：运行 Bash 命令前，先问一句"要执行 `xxx`，可以吗？"，特别是涉及 `rm`、`git push --force`、`curl | sh`、修改全局配置、写入非项目目录等不可逆或有副作用的命令。
- 第一次改完后把改动概括告诉用户，不要反复请示后续同类操作。
